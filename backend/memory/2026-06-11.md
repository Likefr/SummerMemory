# 2026-06-11 工作日志

## ☀️ 白天（公司）— 学生端 + 电子海图 + 语音识别

### 1. 学生端 3dOnlineInteraction
- 实现语音识别功能：基于 Vosk，侧边栏按钮触发实时录音
- 参考项目：Manoeuverorder_recognition
- 雷达页面 hgWebJrc 嵌入学生端，通过 Windows Bus 传入本船和目标船数据

### 2. 电子海图 hgEcdis2 — 目标船传递
- 修复目标船数据传递：tshipList 循环逐条发送，替代单条 tshipData
- 路径：D:\Desktop\hgWebEcdis\trunk\hgNewEcdis\hgEcdis2

### 3. 三维视景 3dOnline
- 修复目标船只显示一条 → 循环 emit 所有目标船
- 修复本船/目标船 hdg 偏差 90° 问题

### 4. 学生端 UI 改造
- 本船信息面板：基础数据 + 号灯初始化 tabs
- 船模分类：普通机动船、帆船、渔船、拖带船
- 修复重新导入场景后车和舵位置不复位

### 5. 语音识别项目 Manoeuverorder_recognition2
- 实时录音 → Vosk 识别 → 执行指令（"open radar" 打开雷达）
- 修复打包后报错：BrowserAudioCapture/AudioWorkletProcessor is not defined、404

---

## 🌙 晚上（17:34 起）— 基础设施 + SummerMemory 大修

### 6. Session 断片排查 & 模型配置
- 根因：模型返回空内容 + 上下文太重
- 解决：配置 fallback 模型链（GLM-5.1 → GLM-4.7）

### 7. Jenkins 恢复
- Docker Jenkins 权限修复 + 从备份恢复
- 地址：http://192.168.31.113:8093

### 8. SummerMemory 图谱数据大修
- 清理 4.7 倍重复数据（776 条重复 + 109 条绝对路径 + 30 条 dreaming）
- 修改 memory_server.py：统一相对路径、过滤 dreaming 目录

### 9. SummerMemory 推送 GitLab + GitHub
- GitLab：summer-memory/backend + summer-memory/frontend
- GitHub：公开仓库 github.com/Likefr/SummerMemory
- 写了超详细 README

### 10. GitHub Profile README 更新

### 11. 短信验证码转发 App 决策确认

---

## 🌙🌙 深夜 — SummerMemory 系统性 Bug 修复

### 12. Ollama Embedding 500 错误
- **根因**：embedding 截取 2000 字符超过 bge-small-zh-v1.5 的 512 token 上限
- **修复**：截取长度从 2000 → 800 字符

### 13. 分块（Chunking）机制实现
- 每个文件按 Markdown 标题分段，每块 ≤800 字符
- 77 个文件 → 736 个 chunks
- 数据库新增 chunk_index 列
- 搜索精度大幅提升

### 14. WebSocket 端口被改（WS 端口 8890 → 11436）
- **根因**：分块子任务擅自改了端口，FRP 穿透还是 8890
- **修复**：改回 8890，和 FRP 配置对齐
- **教训**：端口/路径/配置不许乱改（红线 #12）

### 15. search 广播 path 为 null
- **根因**：search() 调用 _log_activity 时没传 path
- **修复**：把搜索结果的第一个 path 传给 activity

### 16. pulseNode 居中放大 + fitView 自动适配
- pulseNode 加了 zoomToPoint 居中放大（和点击节点效果一致）
- _previewFitTimer 从局部变量提升到 setup 作用域，fitView 10 秒后生效

### 17. 公网部署路径搞错
- 一直更新本地 docker nginx，没更新公网服务器
- **正确路径**：47.119.123.204:/www/wwwroot/ai.likefr.com/graph/
- **部署脚本**：/tmp/deploy_graph.py

### 18. 代码推送到 GitLab + GitHub（进行中）

---

## ⚠️ 新增红线
- #12 端口/路径/配置不许乱改
- #13 禁止用 session 文件/会话历史回忆工作内容

## 📊 SummerMemory 当前状态
- 文件：77 个 → chunks：736 个
- 搜索：~30ms
- 图谱：77 节点 + 3100 边
- 动画：检索/写入自动触发节点居中放大 + 10s 适配
